using System;
class Program
{
    static void Main()
    {
        int a = 10, b = 20, c = 15;
        int max = (a > b && a > c) ? a : (b > c ? b : c);
        Console.WriteLine("Max = " + max);
    }
}
